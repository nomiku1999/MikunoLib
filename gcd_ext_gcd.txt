// x * a + y * b == gcd(x, y)
// return {gcd, x, y}
template <typename T>
tuple<T, T, T> ext_gcd(T a, T b){
	if(!b) return{a,1,0};
	T g, x, y;
	tie(g, x, y) = ext_gcd(b, a % b);
	return {g, y, x - a / b * y};
}


// gcd
template <typename T>
T gcd(T a, T b) { return b == 0 ? a : gcd(b, a % b); }